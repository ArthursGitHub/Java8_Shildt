------------------ Лямбда выражения ---------------------

//listing 1
// Самое простое лямбда-выражение

//listing 2
// Простое лямбда-выражение c одним параметром

//listing 3
// Простое лямбда-выражение c двумя параметрами

//listing 4
// Блочное лямбда-выражение с одним параметром.

//listing 5
// Блочное лямбда-выражение с одним параметром.

//listing 6
// Дженерики в блочном лямбда-выражение с одним параметром.

//listing 7
// Лямбда-выражение передается как аргумент функуии

//listing 8
// Эксепшены в лямбда-выражении

//listing 9
// Пример захвата переменной из объемлющей области действия

-------- Статические методы, методы экземпляра, ссылки на них ----------

//listing 10
// Ссылка на статический метод передается как аргумент функции.

//listing 11
// Ссылка на метод экземпляра передается как аргумент функции.

//listing 12
// Ссылка на метод передается как аргумент функции.
// Причем сам объект передается как ссылка через первый аргумент при вызове метода

//listing 12
// Ссылка на метод передается как аргумент функции.
// Причем сам объект передается как ссылка через первый аргумент при вызове метода

//listing 13
// Ссылка на обобщенный статический метод, передаваемой в качестве аргумента функции

//listing 14
// Ссылка на статический метод, передаваемой в качестве аргумента функции

//listing 15
// Применение ссылки на конструктор.

//listing 16
// Применение ссылки на конструктор обобщенного класса.

//listing 17
// Применение ссылки на конструктор обычного и обобщенного класса.

//listing 18
// Использование предопределенного функционального интерфейса "Function".

